import 'package:wolf_lab/app/bootstrap/app_config.dart';

/// Wolf Lab bootstrap — no legacy database setup needed.
/// Hive boxes are opened directly in main.dart.
Future<void> appBootstrap(AppConfig config) async {}
